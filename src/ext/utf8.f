\ utf8 table dump.
\ ------------------------------------------------------------------------

  .( loading utf8.f ) cr

\ ------------------------------------------------------------------------

: .utf8  ( from --- )
  hex
  256 bounds
  do
    i 16 mod 0=
    if
      cr cr
      i 0 <# # # # # # # # # #> type space
    then
    i (utf8) space
  loop ;

\ ========================================================================
